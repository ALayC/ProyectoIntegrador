@{
    ViewData["Title"] = "Acceso denegado";
    Layout = "_Layout";
}

<div class="container-xl py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card shadow-sm">
  <div class="card-body py-5 px-4">
    <div class="mb-3 text-danger">
             <i class="ti ti-lock" style="font-size: 4rem;"></i>
        </div>
               <h1 class="display-5 fw-bold text-danger">403</h1>
  <h2 class="h4 mb-3">Acceso denegado</h2>
        <p class="text-muted mb-4">
 No tenés permisos para acceder a esta sección.<br />
      Contactá al administrador del sistema si creés que esto es un error.
         </p>
   <a asp-controller="Home" asp-action="Index" class="btn btn-azure">
          <i class="ti ti-arrow-left"></i> Volver al inicio
               </a>
      </div>
            </div>
     </div>
    </div>
</div>
